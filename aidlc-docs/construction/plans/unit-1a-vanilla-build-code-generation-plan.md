# Code Generation Plan — Unit 1a (Vanilla Build Enablement)

## Unit Context
- **Requirements covered**: FR-1, NFR-4, NFR-5 (see `requirements.md`)
- **Design source**: `aidlc-docs/construction/unit-1a-vanilla-build/nfr-requirements/`, `.../nfr-design/`
- **Dependencies**: None (first unit in the sequence)
- **Note**: This unit has no business logic / API / repository / frontend layers — it is a real, executed build-environment setup and compile task. The step sequence below replaces the standard template categories with ones that actually apply: Environment Setup, Build Execution, Verification, Documentation.
- **Workspace root**: `D:\trd\Programming\Git\msys2-runtime` (per `aidlc-state.md`)

## What This Plan Actually Does
Unlike a typical code-generation stage, this unit's "generation" is the real execution of Phase 1 from the brief: download and install an isolated MSYS2 toolchain, then build `winsup/cygwin` from an unmodified checkout, then verify it works normally. This involves real downloads, package installs (~1-2GB), and a compile — expect this to take a meaningful amount of wall-clock time. I will run long steps in the background and report progress rather than blocking on them silently.

## Steps

- [x] **Step 1 — Preflight checks**: Confirmed `curl` available (`/mingw64/bin/curl`), 781G free disk, `.build-toolchain/` did not pre-exist, HEAD sha `2345cc9e2343830ef452ef207796ccca0aab3ccd`.
- [x] **Step 2 — Resolve latest MSYS2 base release**: `msys2/msys2-installer` releases API (tag `nightly-x86_64`) — used `msys2-base-x86_64-latest.sfx.exe` (53,088,864 bytes, digest `sha256:03f02a1eaaf27fa0ac517a2812fba9168ac664e5b9519888fdc340a29c85d7b2`, uploaded 2026-07-17). Note: this rolling "latest" nightly tag has no separate published `.sha256` file; used GitHub's own asset-level SHA256 digest (returned by the Releases API) as the checksum source instead — deviation from the plan's literal wording ("published `.sha256` file") but same intent (verify against a checksum GitHub itself computed at upload, independent of the downloaded bytes).
- [x] **Step 3 — Download archive** to `/tmp/msys2dl/msys2-base-x86_64-latest.sfx.exe` (no separate checksum file to download, per Step 2 note).
- [x] **Step 4 — Verify checksum**: `sha256sum` of downloaded file = `03f02a1eaaf27fa0ac517a2812fba9168ac664e5b9519888fdc340a29c85d7b2` — matches GitHub's published digest exactly.
- [x] **Step 5 — Extract to `.build-toolchain/`**: Self-extracted via the sfx archive's own 7-Zip SFX engine (`-y -o.build-toolchain`) — "Everything is Ok". Result: `.build-toolchain/msys64/` (306M).
- [x] **Step 6 — `.gitignore` updated** at project root with `.build-toolchain/` and `_dest/` entries.
- [x] **Step 7 — First-run MSYS2 initialization**: Ran `pacman -Syu --noconfirm` twice via the extracted `usr/bin/bash.exe`. First pass completed gpg key refresh + "Initial setup complete" without needing a shell restart; second pass confirmed "nothing to do". `SigLevel` untouched throughout.
- [x] **Step 8 — Installed toolchain package set** via `pacman -S --noconfirm --needed <exact CI list>`. All 17 requested names resolved (see note below on `msys2-devel`).
- [x] **Step 9 — Captured package manifest**: `pacman -Q` (165 packages) written to `aidlc-docs/construction/unit-1a-vanilla-build/nfr-requirements/toolchain-versions.md`. Note: `msys2-devel` is a virtual name `Provides`'d by `gcc`, not an installed package itself — `pacman -S msys2-devel` correctly resolved to installing `gcc` (confirmed via `pacman -Si`/`pacman -Qi` — this is expected pacman behavior, not a deviation from the package set).
- [x] **Step 10 — Ran `winsup/autogen.sh`**: regenerated `winsup/configure` via the isolated toolchain's own `/usr/bin/aclocal`/`autoconf`/`automake` (confirmed by inspecting `autogen.sh` and the regenerated file's timestamp) — silent success, exit 0.
- [x] **Step 11 — Ran `./configure --disable-dependency-tracking --with-msys2-runtime-commit=2345cc9e2343830ef452ef207796ccca0aab3ccd`** at the repo root. **Deviation investigated and resolved**: the first attempt inherited `MSYSTEM=MINGW64` from the outer shell environment, causing `uname` to report `MINGW64_NT-...` and `config.guess` to detect `build=x86_64-w64-mingw32` — which silently would not configure `winsup` at all (winsup is only relevant for cygwin/msys-like hosts). Diagnosed via direct `config.guess`/`uname` checks, cleaned the bad `config.status`/`Makefile`/`config.log`, and reran with `MSYSTEM=MSYS` explicitly set. This produced `build_alias=x86_64-pc-cygwin` (not `x86_64-pc-msys` as initially expected) — traced to MSYS2's own `/etc/config.site`, which sets `build_alias="${MSYSTEM_CHOST}"`, and MSYS2 defines `MSYSTEM_CHOST=x86_64-pc-cygwin` for the plain `MSYS` personality by design (the base MSYS environment is Cygwin-API-compatible; `x86_64-pc-msys` is not actually MSYS2's own convention for this personality). Confirmed correct: `host_makefile_frag=./config/mh-cygwin` was selected, and the generated top-level `Makefile` contains `configure-target-winsup`/`all-target-winsup` rules (166 references to `winsup`), confirming it is wired up as a target module (target defaults to host since `--target` was not passed) and will be built.
- [x] **Step 12 — Ran `make -j12`**: **Failed first attempt** — `cc1plus: out of memory` on multiple `.cc` files (aggregate memory pressure from 12 concurrent C++ compiles on this machine, ~9.5G free RAM at the time; not a source/toolchain defect). Retried with `make -j4` per the plan's retry-on-build-failure allowance (toolchain left in place, only the build step retried) — succeeded cleanly, exit 0, 207 compile units, no errors in the full log. Produced `x86_64-pc-cygwin/winsup/cygwin/new-msys-2.0.dll` (24,823,137 bytes, unstripped/debug build) plus `libc.a`, `libpthread.a`, `libmsys-2.0.a`, and the rest of the standard Cygwin/MSYS static library set.
- [x] **Step 13 — Ran `make DESTDIR="$(pwd)/_dest" install`**: exit 0, no errors.
- [x] **Step 14 — Located build output**: `_dest/usr/bin/msys-2.0.dll` (24,823,137 bytes). **No `bash.exe` was produced** — `winsup/cygwin`'s own build only produces the runtime DLL; `bash` is a separate MSYS2 source package not part of this repository, so "bash.exe if feasible" (FR-1) resolves to **not feasible from this repo alone** — noted as a scope finding, not a failure.
- [x] **Step 15 — Verified normal (unsandboxed) behavior**: Backed up the isolated toolchain's own working `msys-2.0.dll`, swapped in the freshly-built one, and ran the toolchain's own `bash.exe` against it. `uname -a` reported `3.6.9-2345cc9e-api-357...` — the embedded commit hash `2345cc9e` matches repo HEAD exactly, positively confirming the *freshly built* DLL (not the original) was what actually ran. Ran `echo`, `uname`, `ls /`, `pwd`, a subshell/fork test, file I/O, env vars, a pipe, and an exit-code check — all succeeded. Restored the original `msys-2.0.dll` afterward and re-verified `uname -a` reverted to the original version string (`01d6c708`) — toolchain left intact for Unit 1b.
- [x] **Step 16 — Wrote build summary documentation**: `aidlc-docs/construction/unit-1a-vanilla-build/code/build-summary.md`.

## Execution Notes
- Steps 7-13 involve real downloads/installs/compilation and may take from several minutes to over an hour depending on network and CPU — these will run with progress updates, not silently.
- If any step fails, per NFR Design's Clean-Slate Retry pattern: for toolchain setup failures (Steps 1-9), delete `.build-toolchain/` and restart from Step 2. For build failures (Steps 10-15), the toolchain itself is left in place (it's not implicated) and only the build steps are retried/debugged.
- No source files under `winsup/` are modified in this unit — confirmed no application-code changes for Unit 1a.
